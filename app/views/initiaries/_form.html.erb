<%= form_for(@initiary) do |f| %>
  <% if @initiary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@initiary.errors.count, "error") %> prohibited this initiary from being saved:</h2>

      <ul>
      <% @initiary.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
